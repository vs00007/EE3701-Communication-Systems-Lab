This directory contains all the efforts we put into building an adaptive OFDM system. 
The `Hardware_RX.m` code runs the receiver with a call to `OFDM_rx2.m` and the `Hardware_TX.m` code runs the transmitter. The codes are well commented and since you people are smart, you should be able to figure it out. But, you shouldn't be here anyway.
